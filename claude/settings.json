{
  "model": "sonnet",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash '/Users/adrianofsantos/.claude/hooks/herdr-agent-state.sh' session",
            "timeout": 10
          }
        ]
      }
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "bash /Users/adrianofsantos/.claude/statusline-command.sh"
  },
  "enabledPlugins": {
    "swift-lsp@claude-plugins-official": false,
    "claude-md-management@claude-plugins-official": true,
    "commit-commands@claude-plugins-official": true,
    "skill-creator@claude-plugins-official": true,
    "hookify@claude-plugins-official": true,
    "warp@claude-code-warp": true,
    "daily.dev@daily.dev": true
  },
  "extraKnownMarketplaces": {
    "claude-code-warp": {
      "source": {
        "source": "github",
        "repo": "warpdotdev/claude-code-warp"
      }
    },
    "daily.dev": {
      "source": {
        "source": "github",
        "repo": "dailydotdev/daily"
      }
    }
  },
  "tui": "fullscreen",
  "agentPushNotifEnabled": true,
  "autoMode": {
    "environment": [
      "### Org-wide",
      "**Organization**: None configured",
      "**Primary use of Claude Code**: software development (personal dotfiles / macOS system configuration via nix-darwin + home-manager)",
      "**Cloud provider(s)**: None configured",
      "**Repository visibility**: PUBLIC — github.com/adrianofsantos/dotfiles is a public repository; any push here is publishing",
      "**Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "**Secrets management**: git-crypt for `nix/user.nix` (encrypted at rest); gitleaks pre-commit hook blocks secret commits; `.gitignore` excludes `*.key` and `.env`",
      "**Default / protected branches**: `main` is the default branch. Ruleset on `main` is disabled and no classic branch protection is listed — treat `main` as unprotected; take extra care with force-push and history-rewriting operations",
      "**CI/CD deploy targets**: None configured",
      "**Network posture**: None configured",
      "**Source control**: The trusted repo and its remote (github.com/adrianofsantos/dotfiles) only — no additional orgs configured",
      "**Trusted internal domains**: None configured",
      "**Trusted cloud buckets**: None configured",
      "**Key internal services**: None configured",
      "**Internal package registry**: None configured",
      "**Sensitive data locations & audiences**: `nix/user.nix` (git-crypt encrypted personal data: username, email, GPG key, paths); any `.env` or `*.key` files (gitignored); GPG key `16D7D0D901DE83FB`; share only with audiences cleared at the [named+specifics] bar",
      "**Data retention / declassification**: None configured",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries `prod` or `production` as a whole word or name segment",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment",
      "### User-specific",
      "**Primary use of Claude Code**: personal dotfiles management (Nix/nix-darwin/home-manager) for two macOS hosts (Aang, Kyoshi)",
      "**Trusted repo**: /Users/adrianofsantos/repos/github/dotfiles and its remote github.com:adrianofsantos/dotfiles.git — PUBLIC repo, so only this repo's own work belongs here; content ported or first read from elsewhere is not cleared for commit/push here, and secrets/personal data (`nix/user.nix`, GPG keys, `.env`, `*.key`) are never cleared for commit regardless of visibility",
      "**Org-specific CLIs**: None configured — routine project CLIs observed: `nix`, `darwin-rebuild` (via `dr`/`dr-build`/`dr-check` aliases), `gpgconf`, `launchctl`, `ssh-add`, `defaults`, `ghostty`, `hostname`",
      "routine under /Users/adrianofsantos/repos/github/dotfiles/ prefix: Nix module edits, `nix flake check`, `dr`/`dr-build`/`dr-check`, gitleaks security-audit scripts, home-manager dotfile symlink management"
    ]
  },
  "theme": "dark-daltonized",
  "preferredNotifChannel": "ghostty",
  "inputNeededNotifEnabled": true
}
